# Autogenerated requirements.txt — regenerated from project imports

# Runtime dependencies
pygame-ce>=2.4.0
Pillow>=9.0.0
requests>=2.28.0

# Web build / runtime helper
pygbag>=0.9.2

# Test / CI tools
pytest>=7.0
playwright>=1.55.0
pytest-timeout>=2.2.0

# Notes:
# - The project imports `pygame` (web builds may use a vendored pygame wheel),
#   `PIL` (Pillow), and `requests` at runtime.
# - `pygbag` is used for producing the web APK; adjust the version if you need
#   a specific pygbag release for your build tooling.
